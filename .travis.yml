language: node_js
cache:
  directories:
    - ~/.npm
node_js:
  - "v12.19.0"
  - "v14.14.0"
  - "v15.0.0"
notifications:
  email: false
stages:
  - lint
  - name: deploy
    if: branch = master
jobs:
  include:
    - stage: lint
      name: eslint
      script: npx eslint .
    - stage: lint
      name: commitlint
      before_script:
        - npm i -g @commitlint/travis-cli
      script: commitlint-travis
    - stage: deploy
      script: npx semantic-release
